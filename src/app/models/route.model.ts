

export class Route {
    id: string
    shortName: string
    longName: string
    description: string

    // Probably not needed but just case
    color: string
    textColor: string

    constructor(
        id: string,
        shortName: string,
        longName: string,
        description: string,

        color: string,
        textColor: string,
    ){}

}